<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright (C) 2011 The Android Open Source Project
    modified
    SPDX-License-Identifier: Apache-2.0 AND GPL-3.0-only
-->

<merge
    xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout
        android:id="@+id/suggestions_strip_wrapper"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hapticFeedbackEnabled="false"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:soundEffectsEnabled="false">
        <!-- Provide audio and haptic feedback by ourselves based on the keyboard settings.
             We just need to ignore the system's audio and haptic feedback settings. -->
        <ImageButton
            android:id="@+id/access_point_trigger_btn"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:padding="0dp"
            android:scaleType="center"
            android:background="@drawable/toolbar_expand_key_background"
            android:src="@drawable/ic_access_point_grid"
            style="?attr/suggestionWordStyle"
            android:contentDescription="Access Point Menu" />
        <FrameLayout
            android:id="@+id/suggestions_middle_container"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="0dp"
            android:layout_marginEnd="0dp"
            android:clipChildren="true"
            android:clipToPadding="true">
            <HorizontalScrollView
                android:id="@+id/toolbar_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scrollbarThumbHorizontal="@color/toolbar_scrollbar"
                android:visibility="gone">
                <LinearLayout
                    android:id="@+id/toolbar"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    style="?attr/suggestionWordStyle">
                </LinearLayout>
            </HorizontalScrollView>
            <!-- Provide audio and haptic feedback by ourselves based on the keyboard settings.
                 We just need to ignore the system's audio and haptic feedback settings. -->
            <LinearLayout
                android:id="@+id/suggestions_strip"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_horizontal"
                android:hapticFeedbackEnabled="false"
                android:soundEffectsEnabled="false" />
            <helium314.keyboard.latin.suggestions.ElasticHorizontalScrollView
                android:id="@+id/suggestions_chip_scroll"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipChildren="true"
                android:clipToPadding="true"
                android:fillViewport="false"
                android:fadingEdgeLength="16dp"
                android:overScrollMode="never"
                android:requiresFadingEdge="horizontal"
                android:scrollbars="none"
                android:visibility="gone">
                <LinearLayout
                    android:id="@+id/suggestions_chip_strip"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:hapticFeedbackEnabled="false"
                    android:soundEffectsEnabled="false" />
            </helium314.keyboard.latin.suggestions.ElasticHorizontalScrollView>
            <androidx.compose.ui.platform.ComposeView
                android:id="@+id/pinned_keys_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:minWidth="0dp"
                style="?attr/suggestionWordStyle" />
        </FrameLayout>
        <ImageButton
            android:id="@+id/persistent_toolbar_key"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:padding="0dp"
            android:scaleType="center"
            android:background="@drawable/toolbar_expand_key_background"
            style="?attr/suggestionWordStyle" />
    </LinearLayout>
</merge>
