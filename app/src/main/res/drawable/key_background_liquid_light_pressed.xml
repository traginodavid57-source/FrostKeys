<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-only -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shifted down for tactile capsule depression, higher fill opacity and specular highlight -->
    <item android:top="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="11dp" />
            <gradient
                android:angle="270"
                android:startColor="@color/liquid_light_key_pressed_start"
                android:endColor="@color/liquid_light_key_pressed_end"
                android:type="linear" />
            <stroke
                android:width="1dp"
                android:color="@color/liquid_light_key_specular_stroke_pressed" />
        </shape>
    </item>

    <!-- Intensified specular sheen on pressed state -->
    <item
        android:left="1dp"
        android:top="3dp"
        android:right="1dp"
        android:bottom="20dp">
        <shape android:shape="rectangle">
            <corners
                android:topLeftRadius="10dp"
                android:topRightRadius="10dp"
                android:bottomLeftRadius="2dp"
                android:bottomRightRadius="2dp" />
            <gradient
                android:angle="270"
                android:startColor="@color/liquid_light_key_specular_top_pressed"
                android:endColor="#00FFFFFF"
                android:type="linear" />
        </shape>
    </item>
</layer-list>
